import React from "react";

import { css } from "@emotion/react";

export const Loading = () => {
  return (
    <div
      css={css`
        width: 100%;
        text-align: center;
      `}
    >
      Loading...
    </div>
  );
};
